/**
 * Проверка реквизитов для API — точка импорта, а НЕ реализация.
 *
 * Арифметика контрольных сумм живёт в `@id/contracts` (`identifiers.ts`), и это
 * не вопрос вкуса. До этой правки её было две: здесь и в обезличивателе корпуса
 * (`tools/fixtures/src/anonymize.ts`). Веса и модули совпадали до цифры, поэтому
 * расхождение было бы молчаливым: §1.4 требует, чтобы обезличиватель сохранял
 * валидность реквизита ровно так, как её понимает портал, — разойдись копии,
 * «битый ОГРН» производного корпуса стал бы валидным для проверок, и
 * регрессионный тест на этот дефект перестал бы проверять что-либо, не покраснев.
 *
 * Поэтому здесь только реэкспорт: ДОБАВЛЯТЬ В ЭТОТ ФАЙЛ ЛОГИКУ НЕЛЬЗЯ — тогда
 * копий снова станет две. Обёртки `isValidInn`/`isValidOgrn`/`isValidKpp` убраны:
 * продакшн различает причину отказа (`length` против `checksum` — это `fail`
 * против `undetermined` в §9.2), а `boolean` эту причину теряет.
 */
export { checkInn, checkKpp, checkOgrn, type IdentifierCheck } from '@id/contracts';
