/**
 * Схема БД и пул соединений.
 *
 * Схема в `generated/` СГЕНЕРИРОВАНА из SQL-миграций и правится только через
 * них: стандарт объявляет миграции источником правды для production, поэтому
 * рукописная копия схемы неизбежно расходилась бы с БД. Первая попытка писать
 * её параллельно дала 39 расхождений в колонках и нулевое пересечение по именам
 * ограничений, причём часть ломала runtime.
 *
 * Ограничения CHECK в схеме ПРИСУТСТВУЮТ — их печатает drizzle-kit при
 * интроспекции, — но вторым источником правды не являются: исполняет их база,
 * а в схему они попадают тем же путём, что колонки, и расхождение с SQL ловит
 * `schema-drift.test.ts`. Пользоваться ими стоит ровно для одного: сверить с
 * ними закрытое перечисление, живущее в коде типом (`match-basis.test.ts`).
 * Тип рантайма не имеет, и до такой сверки код с базой расходились молча — S53
 * добавил основание кандидата без миграции и остановил конвейер на бою.
 */

export * from './generated/schema.js';

export { createPool, closePool } from './pool.js';
export type { PoolOptions } from './pool.js';
export { pgConnectionOptions } from './pg-ssl.js';
