/**
 * Типы для генератора сид-миграции каталога видов ИД
 * (`tools/scripts/generate-seed-migration.mjs`).
 *
 * По образцу `apps/worker/src/jobs/generate-analysis-prompts-seed-migration.d.ts`
 * и по той же причине: скрипт живёт вне рабочего дерева пакета
 * (`tools/scripts/`, не под `rootDir: ./src`) и написан обычным `.mjs`-модулем
 * без своего `.d.ts`. Wildcard-специфайер, а не точный относительный путь:
 * правило матчится по имени файла независимо от глубины импорта.
 *
 * Импортируется ровно ради `TARGET`: тест на дрейф обязан смотреть на ТУ
 * миграцию, в которую пишет генератор. Каталог переезжает новым файлом при
 * каждом изменении (применённая миграция защищена контрольной суммой), и
 * константа в тесте разъехалась бы с генератором на первом же переезде.
 */
declare module '*/generate-seed-migration.mjs' {
  /** Абсолютный путь к последней сид-миграции каталога. */
  export const TARGET: string;
}
