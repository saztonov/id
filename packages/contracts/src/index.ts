/**
 * Общие контракты: zod-схемы и выведенные из них типы.
 *
 * Единственный источник правды по форме данных для backend и frontend.
 * Схема Drizzle (S2) реализует эти сущности в БД, а не наоборот: инварианты
 * вроде диапазона координат блока описаны здесь и продублированы в БД
 * ограничениями `CHECK`, чтобы их держал не только код приложения.
 */

/** Версия набора контрактов. Меняется при несовместимых правках схем. */
export const CONTRACTS_VERSION = 1 as const;

export type ContractsVersion = typeof CONTRACTS_VERSION;

export * from './enums.js';
export * from './domain.js';
export * from './api.js';
/**
 * Описание колонок массового ввода справочников: одно на разборщик в воркере,
 * шаблон в API и предпросмотр на экране (см. заголовок `catalog-import.ts`).
 */
export * from './catalog-import.js';
/**
 * Контрольные суммы реквизитов. Здесь, а не в API: ту же арифметику считают вход
 * справочника, обезличиватель корпуса и движок правил, а две копии расходятся
 * молча (см. заголовок `identifiers.ts`).
 */
export * from './identifiers.js';
/**
 * Формат листа и режим его разметки. Здесь, а не в API: правило считают воркер,
 * API и браузер, а браузер импортировать из `apps/api` не может вовсе
 * (см. заголовок `sheet-format.ts`).
 */
export * from './sheet-format.js';
