# DDL/migrate URL для deploy-id --migrate. Копия: /etc/id/id-migrate.env (640 root:docker).
# На этапе 1 допустима та же роль fin_id, что и в runtime (/etc/id/id.env).
# В git НЕ коммитится. Не печатать в чат/лог.
MIGRATE_DATABASE_URL=postgres://fin_id:ПАРОЛЬ@<host>:6432/ID?sslmode=verify-full
